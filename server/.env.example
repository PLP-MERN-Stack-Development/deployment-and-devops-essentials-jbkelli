# Environment Configuration
# Copy this file to .env and fill in your actual values

# Application
NODE_ENV=development
PORT=5000

# Database - MongoDB Atlas connection string
# Format: mongodb+srv://<username>:<password>@<cluster-url>/<database>?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://jbkelli:katekatie2006@jbkelli.x5aauu7.mongodb.net/

# Frontend URL - update this based on where your frontend is deployed
# For multiple URLs, separate with commas (no spaces after commas)
# Example: https://app.vercel.app,https://app-preview.vercel.app,http://localhost:3000
CLIENT_URL=http://localhost:3000

# JWT Secret - use a strong random string in production
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=0cd711d687f90a56a86cade743a1a69c5f0b6695d18108215d1b64c2af8ec97d

# JWT Expiration
JWT_EXPIRE=7d

# API Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info

# For production deployment on Render/Railway/Heroku:
# Set these environment variables in your hosting platform dashboard
# MONGODB_URI=your-mongodb-atlas-connection-string
# JWT_SECRET=your-secure-random-secret
# CLIENT_URL=https://your-frontend-domain.com
# NODE_ENV=production
